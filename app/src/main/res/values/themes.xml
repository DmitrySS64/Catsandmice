<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CatsAndMice" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CatsAndMice" parent="Base.Theme.CatsAndMice" />

    <style name="Base.Theme.CatsAndMice.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/mouse_icon</item>
        <!-- <item name="windowSplashScreenAnimationDuration">1000</item> -->
        <item name="postSplashScreenTheme">@style/Theme.CatsAndMice</item>
    </style>

    <style name="Theme.CatsAndMice.Splash" parent="Base.Theme.CatsAndMice.Splash">
        <item name="windowSplashScreenBackground">@color/white</item>
    </style>
</resources>